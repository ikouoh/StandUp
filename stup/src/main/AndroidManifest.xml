<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ibyscus.standup.stup" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ibyscus.standup.stup.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ibyscus.standup.stup.GameActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_game"
            android:parentActivityName="ibyscus.standup.stup.MainActivity"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name="ibyscus.standup.stup.MainOptions"
            android:label="@string/title_activity_main_options"
            android:parentActivityName="ibyscus.standup.stup.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ibyscus.standup.stup.MainActivity" />
        </activity>
    </application>

</manifest>
